<% @posts.each do |post| %>
  <div class="posts-index-item">
    <%= link_to gravatar_for(post.user),  "/users/#{post.user.id}" %>
    <%= link_to(post.user.name, "/users/#{post.user.id}") %>
    <%= link_to(post.content, "/posts/#{post.id}") %>
  </div>
<% end %>
